On Master Connect via SSM (for jenkins_admin_password)

/home/ubuntu/jenkins_admin_password.txt

Future:-
Fixed Exposed Ports and Ips

sudo cat /home/jenkins/agent.log

terraform destroy -auto-approve
terraform apply -auto-approve

